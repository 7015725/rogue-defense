# M0.1 Validation Checklist

本文件记录 M0.1 基线验证状态。

## 自动检查

- [x] `npm install` 成功
- [x] `npm run build` 成功
- [x] TypeScript 严格检查通过
- [x] Vite 生产构建通过

> GitHub Actions CI 已完成上述自动检查。手动运行与手感验证仍需在浏览器中完成。

## 手动运行检查

- [ ] 1000×1600 逻辑战场正确缩放
- [ ] 敌人从顶部生成并向底部推进
- [ ] 基础自动炮优先攻击 `pathProgress` 最大的射程内目标
- [ ] 炮塔可旋转、射击、耗尽弹匣并 Reload
- [ ] ProjectilePool 正常回收 Projectile
- [ ] Wave 之间不会清除残余敌人
- [ ] 敌人抵达 Base Attack Line 后停止移动并持续攻击基地
- [ ] Wave 10 进入 Boss Gate
- [ ] Boss 击杀后显示 M0.1 Test Complete
- [ ] `1` / `2` / `3` / `4` 可调整模拟速度
- [ ] `R` 可重新开始

## 性能目标

后续压力测试目标：

```text
300 Active Enemies
+
4× Speed
```

在 Web 浏览器保持可玩。
