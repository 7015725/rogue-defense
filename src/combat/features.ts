// Temporary gameplay feature switches.
// Keep the flying enemy assets and targeting code intact so the feature can be
// restored later without a save migration or combat rewrite.
export const AIR_ENEMIES_ENABLED = false;
